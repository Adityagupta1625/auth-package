import { Auth } from './src/auth';

/**
 * Default export for the Auth module.
 * This module provides authentication-related functionalities.
 *
 * @module Auth
 * @see {@link Auth} - The Auth class or module for authentication.
 */
export {Auth};
