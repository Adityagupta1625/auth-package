import { Auth } from './src/auth'

/**
 * Default export for the Auth module.
 * This module provides authentication-related functionalities including OTP generation and verification.
 *
 * @module Auth
 * @see {@link Auth} - The Auth class for handling OTP generation and verification.
 */
export default Auth
